- Several bugfixes
- More information in the output

The binaries are statically linked to the opencv library, so 
that it does not need to be installed separately. They were 
compiled on several Ubuntu Desktop 64bit and macOS systems 
(here latest=10.15 Catalina).
